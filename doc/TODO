- quoted string values testen (quotes im string richtig ausgegeben?)

- export funktion im ccseditor

- fortschrittsbalken (lexing, parsing, evaluating, generating graph, layouting graph)

- Klammersetzung kontrollieren (Reihenfolge der Operatorauswertung, es sind neue Operator dazugekommen)
  -> values bei input/output actions

- jede Menge falsch getypte arithmetische ausdrücke ausprobieren (channels einsetzen, integer, bool, string durchprobieren)

- preference page aufrufen

- autoupdate in rcp

- switch fuer left-to-right und top-to-bottom
